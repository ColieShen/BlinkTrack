name: pose

_target_: loader.loader_eds_pose.PoseDataModule

root_dir: <path>
dataset_type: EDS
n_frames_skip: 8
n_event_representations_per_frame: 3

num_workers: 4
batch_size: 16
n_train: 2500
n_val: 200
